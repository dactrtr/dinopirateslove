{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Play (Local love.app)",
            "type": "lua-local",
            "request": "launch",
            "program": {
                "command": "${workspaceFolder}/love.app/Contents/MacOS/love"
            },
            "args": [
                "${workspaceFolder}/source"
            ],
            "scriptRoots": [
                "${workspaceFolder}/source"
            ]
        }
    ]
}
